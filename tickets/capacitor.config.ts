import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tonycomm.tcomticketing',
  appName: 'TCOM Ticketing',
  webDir: 'build'
};

export default config;




